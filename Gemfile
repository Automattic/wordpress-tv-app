# frozen_string_literal: true

source 'https://rubygems.org'

# Fastlane is the entry point for all of this project's tooling (build, test, and
# — once signing and App Store Connect are wired up — release lanes).
gem 'fastlane', '~> 2.228'

# Automattic's release toolkit — provides the `configure_*` actions that manage
# secrets via the mobile-secrets repo (configure_apply / download / update / validate).
gem 'fastlane-plugin-wpmreleasetoolkit', '~> 14.7'
