# The Google Play package name of the Android/Google TV app. Note this is the
# Gradle `applicationId` (android/app/build.gradle.kts) — it differs from the
# `namespace`/iOS bundle id `com.automattic.wordpresstv`.
package_name('tv.wordpress')
