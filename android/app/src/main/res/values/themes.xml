<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Activity shell theme. All real styling is done in Compose (tv-material3);
         this just gives a black, chromeless window for the 10-foot UI. -->
    <style name="Theme.WordPressTV" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
