<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- A TV app: there's no touchscreen, and we prefer the leanback (10-foot)
         experience. required="false" on both keeps the app installable on
         phones/tablets too, which is handy for development. -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:banner="@drawable/banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WordPressTV">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|screenLayout|uiMode"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.WordPressTV">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- The TV home-screen tile (renders the banner above). -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
                <!-- Also show in a phone/tablet launcher for development. -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
