# Repo-wide ignores only. Platform-specific ignores live in apple/.gitignore
# and android/.gitignore.

# macOS (can appear in any directory)
.DS_Store

# Local working notes (not part of the public repo)
spec/

# Shared Kotlin Multiplatform build output
shared/build/
shared/.gradle/
