#!/bin/bash -eu

# Placeholder for the Google TV (Android) app.
#
# The Android project under `android/` is not scaffolded yet — see
# `android/README.md`. Once a Gradle project lands, replace the body below with
# the real build/test (e.g. `./gradlew :app:assembleDebug :app:testDebugUnitTest`)
# and move this step onto a Linux agent with the Android SDK (queue: android),
# since Gradle does not need — and should not occupy — the macOS build fleet.
echo "--- :robot_face: Google TV (Android) — not scaffolded yet, skipping"
echo "TODO: build & test the Android app once android/ has a Gradle project."
